---
title: 'Hello, world'
description: 'A placeholder draft showing the post format. Set draft to false to publish.'
pubDate: 2026-06-13
draft: true
tags: ['meta']
---

This is a draft. It will **not** appear on the site or in the RSS feed while
`draft: true`.

To publish a real post: copy this file, give it a new name (the filename becomes
the URL slug), fill in the frontmatter, set `draft: false`, and write Markdown
below.

```ts
// code blocks render with the terminal theme
const ship = (post: string) => deploy(post);
```
