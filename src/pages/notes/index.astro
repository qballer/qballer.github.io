---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog', ({ data }) => !data.draft)).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

const fmt = (d: Date) =>
  d.toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' });
---

<BaseLayout
  title="Notes"
  description="Notes on platform engineering, distributed systems, search and data infrastructure."
>
  <div class="wrap prose">
    <p class="eyebrow">// notes</p>
    <h1>Notes</h1>
    <p class="lede">
      Writing on platform engineering, distributed systems, search and data
      infrastructure.
    </p>

    {
      posts.length === 0 ? (
        <div class="empty">
          <p class="prompt">ls ./posts</p>
          <p class="empty__msg">No posts yet. First one is in the works.</p>
          <p>
            In the meantime, find me on{' '}
            <a href="https://github.com/qballer">GitHub</a> or{' '}
            <a href="https://www.linkedin.com/in/doron-tsur">LinkedIn</a>.
          </p>
        </div>
      ) : (
        <ul class="post-list">
          {posts.map((post) => (
            <li>
              <a href={`/notes/${post.id}`}>
                <time datetime={post.data.pubDate.toISOString()}>
                  {fmt(post.data.pubDate)}
                </time>
                <h2>{post.data.title}</h2>
                <p>{post.data.description}</p>
                {post.data.tags.length > 0 && (
                  <ul class="tags" aria-label="tags">
                    {post.data.tags.map((t) => (
                      <li>{t}</li>
                    ))}
                  </ul>
                )}
              </a>
            </li>
          ))}
        </ul>
      )
    }
  </div>
</BaseLayout>

<style>
  .lede {
    color: var(--text-dim);
  }
  .empty {
    margin-top: var(--sp-8);
    border: 1px dashed var(--border-bright);
    border-radius: var(--radius);
    padding: var(--sp-8);
    background: var(--bg-card);
  }
  .empty .prompt {
    font-family: var(--font-mono);
    color: var(--text-dim);
    margin-bottom: var(--sp-2);
  }
  .empty__msg {
    font-family: var(--font-mono);
    color: var(--text-bright);
    font-size: var(--fs-lg);
  }
  .post-list {
    list-style: none;
    margin: var(--sp-8) 0 0;
    padding: 0;
    border-bottom: 1px solid var(--border);
  }
  .post-list > li {
    border-top: 1px solid var(--border);
  }
  .post-list a {
    display: block;
    padding: var(--sp-6) 0;
    color: inherit;
    border: none;
  }
  .post-list a:hover h2 {
    color: var(--accent);
  }
  .post-list time {
    font-family: var(--font-mono);
    font-size: var(--fs-sm);
    color: var(--text-dim);
  }
  .post-list h2 {
    margin: var(--sp-2) 0;
    font-size: var(--fs-xl);
    transition: color 0.15s var(--ease);
  }
  .post-list > li > a > p {
    color: var(--text-dim);
    margin: 0;
  }
  .post-list .tags {
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    gap: var(--sp-2) var(--sp-3);
    padding: 0;
    margin: var(--sp-4) 0 0;
  }
  .post-list .tags li {
    font-family: var(--font-mono);
    font-size: var(--fs-xs);
    letter-spacing: 0.04em;
    color: var(--accent);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    padding: 0.15em 0.6em;
  }
</style>
